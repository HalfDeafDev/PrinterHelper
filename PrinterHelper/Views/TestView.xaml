<Window x:Class="PrinterHelper.Views.TestView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PrinterHelper.Views"
        xmlns:vm="clr-namespace:PrinterHelper.ViewModels"
        mc:Ignorable="d"
        Title="TestView" Height="450" Width="800">

    <Window.DataContext>
        <vm:TestVM />
    </Window.DataContext>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="boolToVis" />
    </Window.Resources>
    
    <StackPanel>
        <TextBlock Text="Printers:" />
        <Grid>
            <TextBlock Text="Printers Loading" Visibility="{Binding IsLoading, Converter={StaticResource boolToVis}}"/>
            <ListView ItemsSource="{Binding Printers}" 
                      Visibility="{Binding IsLoaded, Converter={StaticResource boolToVis}}"
                      SelectionMode="Single"
                      SelectedItem="{Binding SelectedPrinter}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Name}" Margin="2"/>
                            <TextBlock Text="|"/>
                            <TextBlock Text="{Binding HostAddress}" Margin="2"/>
                            <TextBlock Text="|"/>
                            <TextBlock Text="{Binding Port}" Margin="2"/>
                            <TextBlock Text="|"/>
                            <TextBlock Text="{Binding PortName}" Margin="2"/>
                            <TextBlock Text="|"/>
                            <TextBlock Text="{Binding PrinterRelPath}" Margin="2"/>
                            <TextBlock Text="|"/>
                            <TextBlock Text="{Binding TcpPrinterRelPath}" Margin="2"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Name" />
                <TextBox Text="{Binding PrinterModifications.Name, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Text="Host Address"/>
                <TextBox Text="{Binding PrinterModifications.HostAddress, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Text="Port"/>
                <TextBox Text="{Binding PrinterModifications.Port, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Text="Port Name"/>
                <TextBox Text="{Binding PrinterModifications.PortName, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            <StackPanel Grid.Column="1">
                <TextBlock Text="Name" />
                <TextBlock Text="{Binding PrinterModifications.Name}" />
                <TextBlock Text="Host Address"/>
                <TextBlock Text="{Binding PrinterModifications.HostAddress}"/>
                <TextBlock Text="Port"/>
                <TextBlock Text="{Binding PrinterModifications.Port}"/>
                <TextBlock Text="PortName"/>
                <TextBlock Text="{Binding PrinterModifications.PortName}"/>
            </StackPanel>
        </Grid>
        <Button Command="{Binding UpdatePrinter}" Content="Update Printer" />
    </StackPanel>
</Window>
