<Window x:Class="PrinterHelper.Views.TestView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PrinterHelper.Views"
        xmlns:vm="clr-namespace:PrinterHelper.ViewModels"
        mc:Ignorable="d"
        Title="TestView" Height="450" Width="800">

    <Window.DataContext>
        <vm:TestVM />
    </Window.DataContext>

    <StackPanel>
        <StackPanel.Resources>
            <Style TargetType="TextBox">
                <Setter Property="MinWidth" Value="100" />
            </Style>
            <Style TargetType="CheckBox">
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="Margin" Value="5,0" />
            </Style>
        </StackPanel.Resources>
        <StackPanel Orientation="Horizontal">
            <Label Content="Printer Name:"/>
            <TextBox Text="{Binding PrinterNameFilter}" />
            <Label Content="IP:"/>
            <TextBox Text="{Binding IPFilter}" />
            <Label Content="Port:"/>
            <TextBox Text="{Binding PortFilter}" />
            <CheckBox Content="Use Regular Expressions" IsChecked="{Binding UseRegex}" />
        </StackPanel>
        <TextBlock Text="Printers:" />
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="5*"/>
                <RowDefinition Height="3*"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <ListBox
                Grid.Row="0"
                Grid.ColumnSpan="2"
                ItemsSource="{Binding Printers}"
                SelectedItem="{Binding SelectedPrinter}"
                ScrollViewer.VerticalScrollBarVisibility="Visible"
                ScrollViewer.CanContentScroll="False">
                <ListBox.Resources>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Focusable" Value="True" />
                    </Style>
                </ListBox.Resources>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <CheckBox />
                                <StackPanel>
                                    <Label Content="{Binding Name.SolidValue}" FontWeight="Bold"/>
                                    <StackPanel Orientation="Horizontal" Margin="5, 0, 0, 10">
                                        <TextBlock Text="{Binding HostAddress.SolidValue}" />
                                        <TextBlock Text=":" />
                                        <TextBlock Text="{Binding Port.SolidValue}" />
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <StackPanel Grid.Row="1">
                <TextBlock Text="Name" />
                <TextBox Text="{Binding SelectedPrinter.Name.Value, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Text="Host Address"/>
                <TextBox Text="{Binding SelectedPrinter.HostAddress.Value, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Text="Port"/>
                <TextBox Text="{Binding SelectedPrinter.Port.Value, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Text="Port Name"/>
                <TextBox Text="{Binding SelectedPrinter.PortName.Value, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            <StackPanel Grid.Row="1" Grid.Column="1">
                <TextBlock Text="Name" />
                <TextBlock Text="{Binding SelectedPrinter.Name.SolidValue}" />
                <TextBlock Text="Host Address"/>
                <TextBlock Text="{Binding SelectedPrinter.HostAddress.SolidValue}"/>
                <TextBlock Text="Port"/>
                <TextBlock Text="{Binding SelectedPrinter.Port.SolidValue}"/>
                <TextBlock Text="PortName"/>
                <TextBlock Text="{Binding SelectedPrinter.PortName.SolidValue}"/>
            </StackPanel>
            <Button Grid.Row="2" Grid.ColumnSpan="2" Command="{Binding UpdatePrinter}" Content="Update Printer" />
        </Grid>
    </StackPanel>
</Window>
