<Window x:Class="PrinterHelper.Views.TestView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PrinterHelper.Views"
        xmlns:vm="clr-namespace:PrinterHelper.ViewModels"
        mc:Ignorable="d"
        Title="TestView" Height="450" Width="800">

    <Window.DataContext>
        <vm:TestVM />
    </Window.DataContext>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="boolToVis" />
    </Window.Resources>
    
    <StackPanel>
        <TextBlock Text="Printers:" />
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="5*"/>
                <RowDefinition Height="3*"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <ListView
                Grid.Row="0"
                Grid.ColumnSpan="2"
                ItemsSource="{Binding Printers}"
                SelectionMode="Single"
                SelectedItem="{Binding SelectedPrinter}"
                ScrollViewer.VerticalScrollBarVisibility="Visible"
                ScrollViewer.CanContentScroll="False">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Name}" Margin="2"/>
                            <TextBlock Text="|"/>
                            <TextBlock Text="{Binding HostAddress}" Margin="2"/>
                            <TextBlock Text="|"/>
                            <TextBlock Text="{Binding Port}" Margin="2"/>
                            <TextBlock Text="|"/>
                            <TextBlock Text="{Binding PortName}" Margin="2"/>
                            <TextBlock Text="|"/>
                            <TextBlock Text="{Binding PrinterRelPath}" Margin="2"/>
                            <TextBlock Text="|"/>
                            <TextBlock Text="{Binding TcpPrinterRelPath}" Margin="2"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <StackPanel Grid.Row="1">
                <TextBlock Text="Name" />
                <TextBox Text="{Binding PrinterModifications.Name.Value, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Text="Host Address"/>
                <TextBox Text="{Binding PrinterModifications.HostAddress.Value, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Text="Port"/>
                <TextBox Text="{Binding PrinterModifications.Port.Value, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Text="Port Name"/>
                <TextBox Text="{Binding PrinterModifications.PortName.Value, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            <StackPanel Grid.Row="1" Grid.Column="1">
                <TextBlock Text="Name" />
                <TextBlock Text="{Binding PrinterModifications.Name.Value}" />
                <TextBlock Text="Host Address"/>
                <TextBlock Text="{Binding PrinterModifications.HostAddress.Value}"/>
                <TextBlock Text="Port"/>
                <TextBlock Text="{Binding PrinterModifications.Port.Value}"/>
                <TextBlock Text="PortName"/>
                <TextBlock Text="{Binding PrinterModifications.PortName.Value}"/>
            </StackPanel>
            <Button Grid.Row="2" Grid.ColumnSpan="2" Command="{Binding UpdatePrinter}" Content="Update Printer" />
        </Grid>
    </StackPanel>
</Window>
